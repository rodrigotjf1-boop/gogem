# packages/contracts — DTOs/tipos compartilhados

Tipos e DTOs compartilhados entre `apps/api`, `apps/admin` e `apps/kiosk`. **O OpenAPI (gerado pela API) é a fonte da verdade**; este pacote expõe os tipos derivados dele.

- Toda rota nova nasce documentada no OpenAPI (CLAUDE.md §9).
- Breaking change ⇒ nova versão `/v2`.

> Vazio no S0. Preenchido junto com a API (S1–S2) e a API pública (S12+).
