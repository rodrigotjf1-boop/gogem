# apps/admin — variáveis de ambiente (exemplo). Copie para .env.local e ajuste.
# Vite só expõe variáveis com prefixo VITE_ ao cliente.

# Base da API núcleo (apps/api). Prefixo /api/v1 incluído.
VITE_API_URL=http://localhost:3000/api/v1
